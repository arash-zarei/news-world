import React from 'react';

// Gif
import loading from '../../gif/loading_anim.gif'

const Loading = () => {
    return (
        <div>
            <img src={loading} alt="" />
        </div>
    );
};

export default Loading;